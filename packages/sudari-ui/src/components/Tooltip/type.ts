import { Placement } from '@floating-ui/react';

export type interactive = boolean;
export type placement = Placement;
export type { offset } from '../../types/common';
export type { dismiss } from '../../types/common';
export type { animation } from '../../types/common';
